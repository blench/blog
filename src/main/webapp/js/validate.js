/*$(function(){
	//用户名
	$("#username").click(function(){
		var $str = $("#username").val();
		var $reg = /^\w{6,20}$/;
		if($reg.test($str))
		{
			var $span=$("<span>填写符合要求</span>")
			$("#username").append($span);
		}else{
			var $span=$("<span>用户名必须在6-20个字符之间</span>")
			$("#username").append($span);
		}
	});
	
	//密码
	$("#password").click(function(){
		var $str = $("#password").val();
		var $reg = /^\w{6,20}$/;
		if($reg.test($str))
		{
			var $span=$("<span>填写符合要求</span>")
			$("#password").append($span);
		}else{
			var $span=$("<span>用户名必须在6-20个字符之间</span>")
			$("#password").append($span);
		}
	});
});*/
